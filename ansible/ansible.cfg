# ============================================================
# ansible.cfg — GitHub Actions Runner (FIXED)
# Removed transport=winrm — connection is set per inventory
# ============================================================

[defaults]
inventory           = inventory.ini
host_key_checking   = False
retry_files_enabled = False
stdout_callback     = yaml
roles_path          = roles
